"""Bundled example reference files.

These are shipped as package data so `documentcrawler init --with-examples`
can copy them into the user's working directory regardless of how the
package was installed.
"""
